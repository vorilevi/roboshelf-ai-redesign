# Commit-átadás — 2026-08-16

**A commitot neked kell futtatnod terminálból.** A `.git` a homokozóból nem írható (dokumentált korlát, `multi_robot_strategy` munkaszabályok).

---

## ⚠️ ELŐSZÖR: a beragadt zár

```
-rw------- 1 ... 0 Aug  2 20:44 .git/index.lock
```

**Augusztus 2., 20:44** — a döntési naplóban leírt incidens napja. A repó azóta **nem tud commitolni**. A két hét kimaradt munka nem hanyagság, hanem ez.

---

## A parancsok

```bash
cd ~/roboshelf-ai-dev/roboshelf-ai-redesign

# 1. a beragadt zár
rm -f .git/index.lock

# 2. ELLENŐRZÉS — üresnek kell lennie
git ls-files --others --exclude-standard --modified \
  | grep -iE "private|confidential|one.?pager|pitch|\.pptx|index\.html|\.mp4"

# 3. ha a 2. nem írt ki semmit:
git add -A
git status --short | wc -l          # várt: ~241

# 4. commit
git commit -m "feat(shelflife): Booster T1 track + kiértékelő mérce + portolási költségmetrika

- shelflife_t1_scene: T1 29-DoF jelenet, polcmagasság a munkatérből mérve
- shelflife_t1_gripper: Robotiq 2F85 a csuklóra, tájolás visszamérve (X)
- shelflife_t1_show_date: A3/T1, 67 megfelelő kartartás, takarási kapuval
- shelflife_date_decal + date_readable: valódi dátum a talpon, olvashatósági burkológörbe
- shelflife_eval_harness: sikerarány-mérce, hitelesítve (0%/100% önteszt)
- port_cost_meter + metrika: a fejlesztési sebesség először számmal
- gitignore: 6 kategória, ami átcsúszott (köztük 2 bizalmas one-pager)"

# 5. push
git push
```

---

## Amit a `.gitignore`-ba tettem ma

| minta | miért |
|---|---|
| `*One-Pager*.html` · `*One_Pager*.html` | ⚠️ **BIZALMAS.** A régi minta aláhúzással volt (`Roboshelf_One_Pager_*.html`), két fájl viszont **szóközzel** — publikus repón követetlenül álltak. Ugyanaz a hibaosztály, mint a 07-25-i szivárgás. |
| `*.mp4` · `*.mov` | a demo reel bizalmas (döntési napló), a többi újragenerálható |
| `data/lerobot/` · `data/demos/` | 113 MB adathalmaz — HuggingFace-re való |
| `mujoco_menagerie/` | 69 MB vendorolt külső repó, setup script tölti |
| `assets/*/meshes/` | robot mesh-ek, mint a GR1T1-nél |
| `results/shelflife_film*/` · `*.npy` | 145 MB renderelt képkocka |

**A privát SKU-adatok** (`data/cola_private`, `data/doboz_private`, `shelflife_sku_private`) **már korábban is kizárva voltak** — ellenőrizve.

---

## A commit tartalma

**241 fájl · 17 MB** — bizalmas találat: **nincs**.

| | fájl |
|---|---|
| `results/` | 170 — mérési eredmények, renderek, JSON-ok |
| `tools/` | 49 — a Shelf Life eszközök + a költségmérő |
| `src/` | 12 — jelenetek |
| `docs/` | 5 — ténycsomag, SKU-séma, költségmetrika |
| egyéb | 5 |
